TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_USERNAME=unichat_ai_bot

# 1 = polling mode (no webhook needed; recommended). 0 = webhook mode (requires HTTPS endpoint).
POLLING=1

# Only needed when POLLING=0 (webhook mode):
# WEBHOOK_URL=https://your-api-domain/telegram/webhook/   # webhook mode only
# TELEGRAM_WEBHOOK_SECRET=

MONGO_URI=mongodb://localhost:27017/unichat
OPENROUTER_API_KEY=
BOT_PORT=8081
